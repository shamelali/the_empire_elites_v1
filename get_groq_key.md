# Get FREE Groq API Key (Works instantly!)

1. Visit: https://console.groq.com/keys
2. Create account (free)
3. Create API key
4. Copy key (starts with `gsk_`)

Then run:
```bash
cd /media/shamel/DEV_EXT/DEV/projects/LEISH/the_empire_elites_v1
nano .env
# Add: GROQ_API_KEY=gsk_your_key_here
```

Groq is FREE, fast, and works with CrewAI!
